#sensors:
#  - sensor_d405
#sensor_d405:
#  far_clipping_plane_distance: 0.2
#  filtered_cloud_topic: filtered_cloud
#  image_topic: /arm/D405/depth/image_rect_raw
#  max_update_rate: 1.0
#  near_clipping_plane_distance: 0.1
#  padding_offset: 0.03
#  padding_scale: 1.0
#  queue_size: 2
#  sensor_plugin: occupancy_map_monitor/DepthImageOctomapUpdater
#  shadow_threshold: 0.5
sensors:
  - sensor_d405
sensor_d405:
  filtered_cloud_topic: filtered_cloud
  max_range: 1.3 # meters
  max_update_rate: 1.0
  padding_offset: 0.1 # 0.01 doesn't make it smaller
  padding_scale: 1.0
  point_cloud_topic: /camera/camera/depth/color/points
  point_subsample: 1 # 10 doesn't make it smaller
  sensor_plugin: occupancy_map_monitor/PointCloudOctomapUpdater
## Uncomment the line above if you want to see the blocks generated by the depth data